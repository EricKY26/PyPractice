from quadratic import quadratic

a = float(input('Please input the parameters for the equation: a*x^2 + b*x + c = 0. \n' + 'a = '))
b = float(input('b = '))
c = float(input('c = '))

quadratic(a, b, c)

